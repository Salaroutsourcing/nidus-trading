export const metadata = {
  title: "Track Quote / Order",
  description:
    "Check the status of a Nidus Trading quote request or order using your NT reference number.",
  alternates: { canonical: "/track-order" },
};

export default function TrackOrderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
